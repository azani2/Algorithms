#include <iostream>
#define MAX 777

using namespace std;

long long d[MAX][MAX];
int n;
int a[MAX];

long long calc(int i, int j) {
	if (j == n - 3) {

	}
}

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	cin >> n;
	for (int i = 0; i < n; i++) cin >> a[i];

	return 0;
}
